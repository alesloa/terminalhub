// Superseded by web/src/components/Editor/VideoView.tsx — videos now render inline as a "video-preview"
// editor tab (and in the File Browser's Quick Look), not a floating window. This file is dead and safe
// to delete (a hook blocks `rm` from this agent, so it's left as an empty module).
export {};
