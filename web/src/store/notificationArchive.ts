// Unused — superseded by the server-authoritative notification center (the notifications table +
// /ws/notifications sync). Safe to delete this file; left as a stub because the local tooling blocks
// `rm`. No runtime imports.
export {};
